const BASE_API_URL = "https://stacks-node-api.testnet.stacks.co" // switch with mainnet when needed.
const CHAIN_TYPE = "testnet" // switch with mainnet when needed

module.exports = {BASE_API_URL, CHAIN_TYPE}